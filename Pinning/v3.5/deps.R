# This file only exists because the plotting code is in the parent directory.
# Without this, Renv will helpfully note that ggplot2 is unused and print
# "- The project is out-of-sync -- use `renv::status()` for details."
library(ggplot2)
